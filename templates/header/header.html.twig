<nav class="navbar navbar-expand-lg bg-body-tertiary">
	<div class="container-fluid d-flex justify-content-between">
		<div class="ms-5">
			<a class="navbar-brand hundred-twenty" href="{{ path('app_home') }}">ARNAUD MICHANT</a>
		</div>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse nav-bar-style" id="navbarSupportedContent">
			<ul class="navbar-nav  mb-2 mb-lg-0 ms-5 ">
				<li class="nav-item">
					<a class="nav-link bold" href="{{ path('app_home') }}">{{"Acceuil"|upper}}</a>
				</li>
				<li class="nav-item">
					<a class="nav-link bold" href="{{ path('app_plats')}}">{{"Menus"|upper}}</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link bold" href="{{path('app_logout')}}">{{"Se déconnecter"|upper}}</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link bold" href="{{path('app_login')}}">{{"Se connecter"|upper}}</a>
					</li>
				{% endif %}
				{% if 'ROLE_ADMIN' in app.user.roles %}
					<li class="nav-item">
						<a class="nav-link bold" href="{{path('admin')}}">{{"Back-End"|upper}}</a>
					</li>
				{% endif %}

			</ul>
		</div>

	</div>
</nav>
